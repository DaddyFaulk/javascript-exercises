// Check Age In  JavaScript


function checkAge(name, age) {
  // your code here
  output = ''
  if (age < 21) {
    output = 'Go home,' + name + '!';
  }
  else {
    output = 'Welcome' + ' ' + name;
  }
  return output;
  }
  checkAge('Adrian', 22);
  
